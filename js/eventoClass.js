export class Evento {
  constructor(id, img, artista, fecha, hora, programa, cv, tipo) {
    this.id = id
    this.img = img
    this.artista = artista
    this.fecha = fecha
    this.hora = hora
    this.programa = programa
    this.cv = cv
    this.tipo = tipo
  }
}
